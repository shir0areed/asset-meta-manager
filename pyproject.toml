[project]
name = "asset-meta-manager"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-multipart",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
asset_meta_manager = ["static/**"]
